**Fix the error!**

As currently written, the code provided will throw a Reference error. This is because it is trying to access a variable that is in local scope.

Fix the example so that we don't get the reference error, but are still able to print out the total value that was calculated by the function.